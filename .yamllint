---
extends: default

rules:
  # 150 chars should be enough, but don't fail if a line is longer
  line-length:
    max: 180
    level: warning

ignore: |
  group_vars/*/vault.yml
  roles/
  ansible/
  collections/
  host_vars/*/vault.yml
